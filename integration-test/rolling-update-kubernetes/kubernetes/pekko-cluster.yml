#deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: pekko-rolling-update-demo
  name: pekko-rolling-update-demo
spec:
  replicas: 3
  selector:
    matchLabels:
     app: pekko-rolling-update-demo
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate

  template:
    metadata:
      labels:
        app: pekko-rolling-update-demo
        actorSystemName: pekko-rolling-update-demo
    spec:
      containers:
      - name: pekko-rolling-update-demo
        image: integration-test-rolling-update-kubernetes:1.3.3.7
        # Remove for a real project, the image is picked up locally for the integration test
        imagePullPolicy: Never
        #health
        livenessProbe:
          httpGet:
            path: /alive
            port: management
        readinessProbe:
          httpGet:
            path: /ready
            port: management
        #health
        ports:
        # pekko-management bootstrap
        - name: management
          containerPort: 7626
          protocol: TCP
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        # The pod deletion cost will use this var to identify the pod to be annotated
        - name: KUBERNETES_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: REQUIRED_CONTACT_POINT_NR
          value: "3"
        # Agrona requires the --add-opens JVM option to work on recent JDKs
        - name: JAVA_TOOL_OPTIONS
          value: "-XX:InitialRAMPercentage=75 -XX:MaxRAMPercentage=75 --add-opens=java.base/jdk.internal.misc=ALL-UNNAMED"
#deployment
---
#rbac-reader
#
# Create a role, `pod-reader`, that can list pods and
# bind the default service account in the namespace
# that the binding is deployed to to that role.
#

kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: pod-reader
rules:
- apiGroups: [""] # "" indicates the core API group
  resources: ["pods"]
  verbs: ["get", "watch", "list"] # requires "patch" to annotate the pod
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: pod-reader
subjects:
  # Uses the default service account.
  # Consider creating a dedicated service account to run your
  # Apache Pekko Cluster services and binding the role to that one.
- kind: ServiceAccount
  name: default
roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io
#rbac-reader
---
#rbac-annotator
#
# Create a role, `pod-annotator`, that can annotate pods
# with the pod-deletion-cost value
#
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: pod-annotator
rules:
  - apiGroups: [""] # "" indicates the core API group
    resources: ["pods"]
    verbs: ["patch"] # requires "patch" to annotate the pod
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: annotate-pods
subjects:
  - kind: User
    name: system:serviceaccount:pekko-rolling-update-demo-ns:default
roleRef:
  kind: Role
  name: pod-annotator
  apiGroup: rbac.authorization.k8s.io
#rbac-annotator
